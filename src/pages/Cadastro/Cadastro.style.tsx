import styled from 'styled-components';
import { LoginCard, ImageBox, LoginContainer } from '../Login/Login.style';

export const CadastroContainer = styled(LoginContainer)``

export const CadastroCard = styled(LoginCard) ``;

export const CadastroImageBox = styled(ImageBox) `
    img {
        width: 30rem;
    }
`;